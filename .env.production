# Empty on purpose: the app then calls /api/... on its own origin and Netlify
# proxies that to the backend (see scripts/generate-redirects.mjs).
# A VITE_BASE_URL set in the Netlify UI overrides this file and makes the
# browser call the backend directly instead.
VITE_BASE_URL=
